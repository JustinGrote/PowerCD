name: PowerCD Build
on: push

jobs:
  build:
    name: Build
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [
          ubuntu-latest
          windows-latest
          macOS-latest
        ]
    steps:
      - name: Pwsh Test
        shell: pwsh
        run: |
          iex (iwr https://gist.githubusercontent.com/JustinGrote/0ffbe5f514d5d6e5b5a62354a6545738/raw/TestAction.ps1)
          "##[section]StandaloneSection"
          "##[warning]StandaloneWarning"
          "##[group]MyCustomGroup"
          "My custom log output"
          "##[endgroup]"
