name: PowerCD Build
on: push

jobs:
  build:
    name: Build
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [
          #ubuntu-latest
          windows-latest
          #macOS-latest
        ]
    steps:
      - name: Pwsh Test
        shell: pwsh
        run:
          "Test1"
          "Test2 #with comment"
          Test3 #with comment